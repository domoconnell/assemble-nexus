/local
# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude (local-only)
/claude-playground
/claude-tasks
/claude.md

# wallet signing material (the README in this dir is committed; the certs aren't)
/secrets/wallets/*.pem
/secrets/wallets/*.cer
/secrets/wallets/*.p12
